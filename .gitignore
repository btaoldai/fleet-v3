# Rust / Cargo
/target
**/*.rs.bk

# Cargo.lock est conservé (le workspace contient un binaire, bot-root).

# Secrets et runtime
.env
.env.*
!.env.example
*.sock

# Éditeurs / OS
/.idea
/.vscode
.DS_Store
Thumbs.db
